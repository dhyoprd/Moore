---
id: "0006"
title: "Spec contract template — the single source of truth format"
status: open
labels: [wayfinder:ticket, wayfinder:task]
parent: "0001"
assignee: ""
blocked_by: ["0003"]
---

## Question

Create the platform-agnostic spec contract template that will hold every decision.

Must produce:
- A markdown template with sections for: State Machine, Data Schema, Business Rules, API Contract, UI Copy.
- One filled example (Plate Calculator) showing the level of precision required.
- Rules for how agents must read this contract before writing Swift/Kotlin.

This template is what makes "solo + AI agents" viable. No code yet, just the contract format.
